<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>호텔 로봇 서비스</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('/images/forest.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .overlay {
            background-color: rgba(0, 0, 0, 0.5);
            min-height: 100vh;
            padding: 2rem 0;
        }
        
        .kiosk-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        .service-card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            padding: 3rem;
            margin: 1rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
            height: 400px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }
        
        .service-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
            background: rgba(255, 255, 255, 1);
        }
        
        .service-icon {
            font-size: 5rem;
            margin-bottom: 2rem;
            color: #2c5530;
        }
        
        .service-title {
            font-size: 2.5rem;
            font-weight: bold;
            color: #2c5530;
            margin-bottom: 1rem;
        }
        
        .service-description {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 2rem;
            line-height: 1.5;
        }
        
        .service-button {
            font-size: 1.5rem;
            padding: 15px 40px;
            border-radius: 50px;
            border: none;
            background: linear-gradient(45deg, #2c5530, #4a7c59);
            color: white;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(44, 85, 48, 0.3);
        }
        
        .service-button:hover {
            background: linear-gradient(45deg, #4a7c59, #2c5530);
            transform: scale(1.05);
            box-shadow: 0 8px 25px rgba(44, 85, 48, 0.5);
            color: white;
        }
        
        .header-title {
            color: white;
            font-size: 3rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
            margin-bottom: 2rem;
            font-weight: bold;
        }
        
        .status-card {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            padding: 2rem;
            margin: 2rem 0;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
        }
        
        .robot-status {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1rem;
        }
        
        .robot-name {
            font-weight: bold;
            color: #2c5530;
        }
        
        .status-badge {
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9rem;
        }
        
        .available {
            background-color: #d4edda;
            color: #155724;
        }
        
        .busy {
            background-color: #f8d7da;
            color: #721c24;
        }
        
        .welcome-text {
            color: white;
            font-size: 1.3rem;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.7);
            margin-bottom: 3rem;
            text-align: center;
        }
    </style>
</head>
<body>
<!-- 버튼 클릭 시 자바스크립트로 이동 -->
<button onclick="window.location.href='/food_delivery'" class="service-button">배달 서비스</button>
<div class="overlay">
    <div class="kiosk-container">
        <h1 class="text-center header-title">
            <i class="fas fa-robot me-3"></i>호텔 로봇 서비스
        </h1>

        <p class="welcome-text">
            안녕하세요! 저희 호텔의 스마트 로봇 서비스를 이용해보세요.<br>
            필요한 서비스를 선택하시면 로봇이 신속하게 도와드리겠습니다.
        </p>

        <!-- 로봇 상태 표시 -->
        <div class="status-card">
            <h5 class="mb-3"><i class="fas fa-info-circle me-2"></i>로봇 상태</h5>
            <div class="robot-status">
                <span class="robot-name"><i class="fas fa-robot me-2"></i>서빙로봇 Alpha</span>
                <span class="status-badge available">사용가능</span>
            </div>
            <div class="robot-status">
                <span class="robot-name"><i class="fas fa-robot me-2"></i>배달로봇 Beta</span>
                <span class="status-badge available">사용가능</span>
            </div>
            <div class="robot-status">
                <span class="robot-name"><i class="fas fa-robot me-2"></i>청소로봇 Gamma</span>
                <span class="status-badge busy">서비스 중</span>
            </div>
        </div>

        <!-- 서비스 선택 버튼 -->
        <div class="row justify-content-center">
            <div class="col-lg-5 col-md-6">
                <div class="service-card">
                    <i class="fas fa-utensils service-icon"></i>
                    <h2 class="service-title">배달 서비스</h2>
                    <p class="service-description">
                        룸서비스, 음식 배달, 음료 주문 등<br>
                        객실까지 안전하게 배달해드립니다
                    </p>
                    <button class="btn service-button" onclick="selectDeliveryService()">
                        <i class="fas fa-shopping-cart me-2"></i>배달 주문하기
                    </button>
                </div>
            </div>

            <div class="col-lg-5 col-md-6">
                <div class="service-card">
                    <i class="fas fa-box service-icon"></i>
                    <h2 class="service-title">물품 서비스</h2>
                    <p class="service-description">
                        수건, 어메니티, 청소용품 등<br>
                        필요한 물품을 신속하게 제공해드립니다
                    </p>
                    <button class="btn service-button" onclick="selectItemService()">
                        <i class="fas fa-hands-helping me-2"></i>물품 요청하기
                    </button>
                </div>
            </div>
        </div>

        <!-- 이용 안내 -->
        <div class="status-card mt-4">
            <h5 class="mb-3"><i class="fas fa-lightbulb me-2"></i>이용 안내</h5>
            <div class="row">
                <div class="col-md-4 text-center mb-3">
                    <i class="fas fa-clock text-success mb-2" style="font-size: 2rem;"></i>
                    <h6>24시간 서비스</h6>
                    <small class="text-muted">언제든지 이용 가능합니다</small>
                </div>
                <div class="col-md-4 text-center mb-3">
                    <i class="fas fa-shield-alt text-success mb-2" style="font-size: 2rem;"></i>
                    <h6>안전한 배송</h6>
                    <small class="text-muted">무균 처리된 로봇으로 위생적 배송</small>
                </div>
                <div class="col-md-4 text-center mb-3">
                    <i class="fas fa-mobile-alt text-success mb-2" style="font-size: 2rem;"></i>
                    <h6>실시간 알림</h6>
                    <small class="text-muted">객실 전화로 도착 안내</small>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // 배달 서비스 선택 시 처리 로직
    function selectDeliveryService() {
        window.location.href = '/food_delivery';  // 실제 설정한 경로로 이동
    }

    // 물품 서비스 선택 시 처리 로직
    function selectItemService() {
        alert('물품 서비스를 선택하셨습니다.\n물품 선택 페이지로 이동합니다.');
        window.location.href = '/items/select';
    }

    // 로봇 상태 실시간 업데이트 (시뮬레이션)
    function updateRobotStatus() {
        const statusElements = document.querySelectorAll('.status-badge');
        statusElements.forEach((element, index) => {
            if (Math.random() > 0.8) { // 20% 확률로 상태 변경
                if (element.classList.contains('available')) {
                    element.classList.remove('available');
                    element.classList.add('busy');
                    element.textContent = '서비스 중';
                } else {
                    element.classList.remove('busy');
                    element.classList.add('available');
                    element.textContent = '사용가능';
                }
            }
        });
    }

    // 30초마다 로봇 상태 업데이트
    setInterval(updateRobotStatus, 30000);
</script>